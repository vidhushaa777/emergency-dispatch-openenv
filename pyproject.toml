[project]
name = "emergency-dispatch-openenv"
version = "0.1.0"
description = "OpenEnv RL environment for emergency dispatch"
requires-python = ">=3.10"
authors = [
  { name = "Vidhushaa" }
]

dependencies = [
  "fastapi",
  "uvicorn",
  "openenv>=0.1.13"
]

[project.scripts]
server = "app.main:app"